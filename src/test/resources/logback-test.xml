<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Kafka Appender Configuration -->
    <appender name="KAFKA" class="io.github.bullettooth.logback.kafka.KafkaAppender">
        <!-- Use environment variables for Kafka configuration -->
        <property name="bootstrap.servers" value="kafka:9092"/>
        <property name="topic" value="test-topic"/>

        <!-- Specify an encoder for the appender -->
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Console Appender Configuration -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Root Logger -->
    <root level="INFO">
        <appender-ref ref="KAFKA"/>
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>
